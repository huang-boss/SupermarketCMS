jQuery(function($){
    // $(function(){// dom元素加载完毕
    $('#tb tbody tr:even').css("backgroundColor","#F0F3FA");
       //获取id为tb的元素,然后寻找他下面的tbody标签，再寻找tbody下索引值是偶数的tr元素,
       //改变它的背景色.
    // })
    $('')
})